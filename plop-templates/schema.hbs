import { z } from 'zod'

// TODO: Customize os schemas conforme seu modelo {{modelName}}

// Schema base - resposta da API
export const {{modelName}}Schema = z.object({
	id: z.string().uuid(),
	// TODO: Adicione os campos do seu modelo
	// Exemplo:
	// name: z.string(),
	// description: z.string(),
	// active: z.boolean(),
	createdAt: z.date(),
	updatedAt: z.date()
})

// Schema create - POST {{modelPlural}}
export const {{modelName}}CreateSchema = z.object({
	// TODO: Adicione campos obrigatórios
	// Exemplo:
	// name: z.string().min(1, 'Nome é obrigatório').max(255),
	// description: z.string().optional(),
}).strict()

// Schema update - PUT {{modelPlural}}/:id
export const {{modelName}}UpdateSchema = z.object({
	// TODO: Adicione campos opcionais
	// Exemplo:
	// name: z.string().min(1).max(255).optional(),
	// description: z.string().optional(),
	// active: z.boolean().optional(),
}).strict()

// Schema filter - queries avançadas
export const {{modelName}}FilterSchema = z.object({
	// TODO: Adicione campos filtráveis
	// Exemplo:
	// search: z.string().optional(),
	// active: z.boolean().optional(),
	// skip: z.number().optional(),
	// take: z.number().optional(),
})

// Types TypeScript derivados dos schemas
export type {{modelName}} = z.infer<typeof {{modelName}}Schema>
export type {{modelName}}Create = z.infer<typeof {{modelName}}CreateSchema>
export type {{modelName}}Update = z.infer<typeof {{modelName}}UpdateSchema>
export type {{modelName}}Filter = z.infer<typeof {{modelName}}FilterSchema>
